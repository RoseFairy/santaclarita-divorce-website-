// Route group layout kept for organization only.
// Navigation (Navbar + Footer) is now handled in the root layout.
export default function MarketingGroupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
